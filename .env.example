# Cybersharp Prime - Local-First Configuration
# This game runs entirely offline with no external dependencies

# No environment variables are required for the game to run
# All data is stored locally using IndexedDB and localStorage
